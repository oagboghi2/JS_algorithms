
// This algorithm has a big(O) of constant, 
// becuase the n doesn't change.
function addUpTo(n){
    return n * (n + 1) / 2;
}

                                                                                                                      Number of operation is(eventually) bounded by a multiple of n
function addUpTo(n){
    let total = 0;
    for(let i = 1; i <= n; i++){
        total += 1
    }
    return total;
})

function printAllPairs(n){
    for(var i = 0; i < n;i+){
        for(var j = n; i < n; j++
            console.log(i)
    }
}